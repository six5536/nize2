meta {
  name: Update Admin Config
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/admin/config/{{configScope}}/{{configKey}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
  Content-Type: application/json
}

body:json {
  {
    "value": "new-value"
  }
}
