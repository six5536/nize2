meta {
  name: Create Grant
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/permissions/{{resourceType}}/{{resourceId}}/grants
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
  Content-Type: application/json
}

body:json {
  {
    "userId": "{{userId}}",
    "level": "read"
  }
}
